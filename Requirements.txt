# --- Core numerical + scientific ---
numpy>=1.24.0
scipy>=1.10.0

# --- Audio input + streaming ---
sounddevice>=0.4.7
portaudio19-dev  # system-level dependency; not a pip package (install separately if needed)

# --- Network + requests handling ---
requests>=2.31.0
urllib3>=2.0.7
certifi>=2023.7.22
idna>=3.4
charset-normalizer>=3.2.0

# --- Queue/thread safe / concurrency ---
# (uses Python stdlib: queue, threading — no pip dep)

# --- Google Gemini API ---
google-generativeai>=0.5.2
protobuf>=4.25.0
google-api-core>=2.17.0
google-auth>=2.23.0
google-ai-generativelanguage>=0.6.5

# --- Environment + config management ---
python-dotenv>=1.0.1

# --- Sentence embeddings + NLP stack ---
sentence-transformers>=2.5.1
transformers>=4.41.0
torch>=2.1.0
tokenizers>=0.15.0
nltk>=3.8.1
scikit-learn>=1.3.0
tqdm>=4.66.0
huggingface-hub>=0.20.0
pandas>=2.1.0

# --- FAISS similarity search ---
faiss-cpu>=1.7.4
# For GPU users, comment above and use:
# faiss-gpu>=1.7.4

# --- JSON + schema safety ---
orjson>=3.9.5  # optional but faster json parsing
